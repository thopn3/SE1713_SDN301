// Định nghĩa các hàm tính toán
function Add(x, y){
    return x + y;
}

const Sub = (x,y) => {
    // statements ....
    return x - y;
}

const Mul = (x,y) => x * y;

export {
    Add,
    Sub,
    Mul
}